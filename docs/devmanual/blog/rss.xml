<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/">
    <channel>
        <title>Otoroshi Blog</title>
        <link>https://maif.github.io/otoroshi/devmanual/blog</link>
        <description>Otoroshi Blog</description>
        <lastBuildDate>Mon, 24 Aug 2026 00:00:00 GMT</lastBuildDate>
        <docs>https://validator.w3.org/feed/docs/rss2.html</docs>
        <generator>https://github.com/jpmonette/feed</generator>
        <language>en</language>
        <item>
            <title><![CDATA[Otoroshi 18 - new foundations, same gateway]]></title>
            <link>https://maif.github.io/otoroshi/devmanual/blog/otoroshi-18-new-foundations</link>
            <guid>https://maif.github.io/otoroshi/devmanual/blog/otoroshi-18-new-foundations</guid>
            <pubDate>Mon, 24 Aug 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[Otoroshi 18.0.0 is the largest release we have shipped in years. The whole runtime moved to Scala 3, Play 3 and Apache Pekko, and Service Descriptors -- deprecated for four years -- are finally gone.]]></description>
            <content:encoded><![CDATA[<p>Otoroshi <code>18.0.0</code> is the largest release we have shipped in years. The whole runtime moved to Scala 3, Play 3 and Apache Pekko, and Service Descriptors -- deprecated for four years -- are finally gone.</p>
<p>It is also, by design, meant to be one of the most boring upgrades you will ever do. Your routes, plugins, API keys, certificates and exporters are untouched. Your traffic does not care.</p>
<p>And once the new foundations were in, we started building on them: every caller of an API is now an API key, consumers can carry their own plugin chain, any plugin can run conditionally, and the whole gateway went through a security pass.</p>
<!-- -->
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-ship-and-why-it-needed-new-planks">The ship, and why it needed new planks<a href="https://maif.github.io/otoroshi/devmanual/blog/otoroshi-18-new-foundations#the-ship-and-why-it-needed-new-planks" class="hash-link" aria-label="Direct link to The ship, and why it needed new planks" title="Direct link to The ship, and why it needed new planks" translate="no">​</a></h2>
<p>There is an old puzzle about a ship whose planks are replaced one by one until none of the originals remain: is it still the same ship? Philosophers have argued about it for two thousand years. The crew, meanwhile, never stopped sailing.</p>
<p>That is roughly what <code>18.0.0</code> is. Underneath, almost every foundational plank has been swapped. On deck, nothing moved.</p>
<p>The planks needed swapping for reasons that had been accumulating for a while:</p>
<ul>
<li class=""><strong>Scala 2.12</strong> first shipped in 2016. Staying on it meant an ever-shrinking set of libraries willing to publish for it, and no access to anything the language has learned since.</li>
<li class=""><strong>Akka</strong> was relicensed under the BSL in September 2023. The Apache Software Foundation forked the last Apache-2.0 release as <strong>Apache Pekko</strong>, and the ecosystem followed. Otoroshi did not, yet.</li>
<li class=""><strong>Play 2.8</strong> went out of support, and <strong>Play 3.0</strong> -- the Pekko-based line -- landed in late 2023. Issue <a href="https://github.com/MAIF/otoroshi/issues/1755" target="_blank" rel="noopener noreferrer" class="">#1755</a> was opened on November 7th, 2023 and stayed open for almost three years.</li>
<li class="">We were shipping a <strong>patched snapshot build of akka-stream</strong> in the repository (<code>akka-stream_2.12-2.6.21+5-a72bf6ba-SNAPSHOT.jar</code>) to carry a TLS 1.3 handshake fix that upstream had not released.</li>
</ul>
<p>Individually, each of those is survivable. Together they meant the foundation was quietly drifting away from the rest of the JVM ecosystem, and the gap was only going to widen.</p>
<p>So in July we tagged <code>scala2-freeze</code>, stopped adding features, and did the port.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-actually-changed">What actually changed<a href="https://maif.github.io/otoroshi/devmanual/blog/otoroshi-18-new-foundations#what-actually-changed" class="hash-link" aria-label="Direct link to What actually changed" title="Direct link to What actually changed" translate="no">​</a></h2>
<p><strong>Scala 2.12.16 → 3.8.4.</strong> The entire backend, plus the test suite, ported in <a href="https://github.com/MAIF/otoroshi/pull/2595" target="_blank" rel="noopener noreferrer" class="">PR #2595</a>.</p>
<p><strong>Play 2.8.19 → 3.0.11, Akka 2.6 → Apache Pekko 1.6.</strong> Same actor model, same streams, same HTTP stack -- Pekko is a direct fork of the Akka we were already running. The package names changed, the semantics did not.</p>
<p><strong>The vendored third-party jars are gone.</strong> The patched akka-stream snapshot was dropped: the TLS 1.3 fix it carried has been part of Pekko since 1.1.0, so we now run straight upstream Pekko with no local patches. <code>scala-schema</code> went with it, and so did our local build of <code>java-jq</code>, which we had been carrying for Apple Silicon support and which now comes from its upstream release.</p>
<p><strong>The admin UI build moved from webpack to Vite</strong>, and jQuery left the frontend for good. The documentation toolchain was updated along the way. You will not see any of it, which is the point: faster builds and fewer ancient dependencies for the people working on Otoroshi.</p>
<p><strong>Service Descriptors have been removed.</strong> They were deprecated in <code>v1.5.3</code> (February 2022) when the new proxy engine landed, <code>v17.0.0</code> shipped the migration tooling, and <code>v18.0.0</code> removes the entity, its admin API, its UI pages and its Kubernetes CRD. Routes are now the only way to configure HTTP proxying -- one entity, one mental model, one code path. This is the plank we had been carrying the longest, and by far the biggest thing this release takes away.</p>
<p>Six weeks from freeze to the first preview.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="then-we-started-building-on-deck">Then we started building on deck<a href="https://maif.github.io/otoroshi/devmanual/blog/otoroshi-18-new-foundations#then-we-started-building-on-deck" class="hash-link" aria-label="Direct link to Then we started building on deck" title="Direct link to Then we started building on deck" translate="no">​</a></h2>
<p>The rule for the port was strict: change nothing but the foundation. Once it had shipped as a preview and the test suites were green on the new stack, we lifted the rule, and the following previews added a few things on top.</p>
<p><strong>Every API consumer is an API key.</strong> Until now, only <code>apikey</code> and <code>oauth2-local</code> plans of the <a class="" href="https://maif.github.io/otoroshi/devmanual/docs/entities/apis">API</a> entity really knew who was calling. In <code>18.0.0</code>, every published plan turns its caller into an API key, whatever it presents: client credentials, a JWT, a client certificate, an OIDC token, or nothing at all -- a <code>keyless</code> plan identifies its callers with an expression, their IP address by default. Everything an API key carries then applies to the call. A public plan can rate limit its callers one by one, exactly like a plan asking for credentials, and a single <code>Api consumer enforcer</code> at the end of access validation is the only place where calls are counted. The details are in <a class="" href="https://maif.github.io/otoroshi/devmanual/docs/topics/api-consumers">API consumers and plugin flows</a>.</p>
<p><strong>Plugin chains per consumer.</strong> A plan, or a single API key, can now bring its own plugins, applied on top of the chain of the route it calls -- or replacing it. Two consumers of the same route no longer have to go through the same pipeline: the gold plan can get a response cache, a partner key can get an extra header, a trial plan can run a stricter validator. Both chains are edited in a designer drawer, right from the plan or the API key.</p>
<p><strong>Conditional plugins.</strong> The new <a class="" href="https://maif.github.io/otoroshi/devmanual/docs/plugins/conditional-plugin">Conditional plugin</a> wraps any other plugin and runs it only when a set of JSONPath predicates matches -- on the API key, the authenticated user, the route metadata, the request, the response or the request attributes. "Only apply this rate limiter to the free tier" is now configuration, not code. JSONPath evaluation itself got a lot faster in the process, which also benefits the plugins that already relied on it.</p>
<p><strong>A security pass.</strong> Admin API rights are checked more strictly for users and API keys scoped to a tenant or a team. SAML response validation was fixed and is now secure by default. An LDAP filter injection was closed. The token the admin UI hands to the admin API is now short-lived and signed with the Otoroshi secret. Random ids and digest comparisons were hardened, and the keypair verifying an API key JWT can be pinned so that a token cannot pick it through its <code>kid</code> header. We also wrote down everything we would check before putting a gateway in front of real traffic: the new <a class="" href="https://maif.github.io/otoroshi/devmanual/docs/topics/production-checklist">production checklist</a> is worth ten minutes of your time, whether you install Otoroshi or upgrade it.</p>
<p>A few smaller things came along:</p>
<ul>
<li class="">WebAuthn works again, for the admin UI and for authentication modules.</li>
<li class="">Let's Encrypt certificates can be renewed with a fixed margin in days, old copies can be deleted after renewal, and an endpoint collapses the duplicates an instance may have accumulated.</li>
<li class="">Remote catalogs can sync from Bitbucket Server / Data Center, and from any self-hosted forge exposing a compatible API.</li>
<li class="">User Analytics lets you choose which families of events are stored, and admin extensions can contribute their own queries and projections.</li>
<li class="">Info tokens signed with a keypair now carry a <code>kid</code> header, so backends can pick the right key from a JWKS.</li>
<li class="">The test suite grew along the way, with dedicated coverage for clustering, mTLS, WebAuthn, SAML validation and admin API rights.</li>
</ul>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-this-means-for-you">What this means for you<a href="https://maif.github.io/otoroshi/devmanual/blog/otoroshi-18-new-foundations#what-this-means-for-you" class="hash-link" aria-label="Direct link to What this means for you" title="Direct link to What this means for you" translate="no">​</a></h2>
<p>For the vast majority of installations, the answer is: run the new version.</p>
<p><strong>Your Service Descriptors migrate themselves.</strong> On first startup, a job runs once per cluster, writes a backup of every descriptor to <code>./service-descriptors-backup.json</code>, then converts each one to a Route <em>keeping the same id</em>. A descriptor is only deleted once its Route has been written and read back successfully -- for any given id there is always either a Route or a Descriptor in the datastore, never neither. Anything that fails or conflicts is left alone, logged, and retried at the next startup, and keeps serving traffic in the meantime. API keys pointing at <code>service_&lt;id&gt;</code> still work, old exports still import, and the analytics endpoints are unchanged. The full story is in <a class="" href="https://maif.github.io/otoroshi/devmanual/docs/topics/deprecating-sd">Sunsetting Service Descriptors</a>.</p>
<p>A few things are worth checking before you upgrade:</p>









































<table><thead><tr><th>If you...</th><th>then...</th></tr></thead><tbody><tr><td>tuned <code>akka.*</code> settings in your config</td><td>rename them to <code>pekko.*</code> (<code>akka.http.parsing.max-uri-length</code> → <code>pekko.http.parsing.max-uri-length</code>, and so on). The <code>OTOROSHI_AKKA_*</code> environment variables keep working, and now have <code>OTOROSHI_PEKKO_*</code> equivalents</td></tr><tr><td>depend on Otoroshi as a library</td><td>the Maven artifact moves from <code>fr.maif:otoroshi_2.12</code> to <code>fr.maif:otoroshi_3</code></td></tr><tr><td>ship <strong>custom Scala plugins</strong> as JARs</td><td>they must be recompiled against Scala 3 and the new artifact. WASM plugins are unaffected</td></tr><tr><td>deploy on <strong>Kubernetes</strong></td><td>apply the updated CRD manifests -- the <code>ServiceDescriptor</code> CRD is gone, and the <code>ingress.otoroshi.io/is-route=true</code> annotation is no longer read (the Ingress controller always creates Routes now)</td></tr><tr><td>publish <strong>APIs</strong> with <code>keyless</code>, <code>jwt</code>, <code>mtls</code> or <code>oauth2-remote</code> plans</td><td>their callers now become API keys, rate limited by the plan and persisted by default. On a public API with many distinct callers, set <code>create_if_missing</code> to <code>false</code> on the plan so the datastore does not get one API key per caller</td></tr><tr><td>use <strong>SAML</strong> authentication modules</td><td>responses are now really checked against the configured issuer, must carry exactly one assertion, and must be signed when signature validation is on. Existing modules keep their settings; new ones validate signatures and assertions by default</td></tr><tr><td>rely on the status code of <code>NgServiceQuotas</code>, <code>NgCustomQuotas</code> or <code>NgCustomThrottling</code></td><td>they now answer <code>429 Too Many Requests</code> instead of <code>403</code> when a limit is reached</td></tr><tr><td>delegate admin rights to <strong>tenant or team-scoped</strong> users or API keys</td><td>a patch can no longer move an entity outside of the caller's scope, nobody can grant rights they do not hold, and writing the global config from a template requires superadmin rights</td></tr></tbody></table>
<p>One small UI removal: the <code>Use circuit breakers</code> global toggle in the Danger Zone is gone. It only ever existed to switch circuit breakers off for Service Descriptors; circuit breaking is now configured per Route, in the client settings of its backend.</p>
<p>If you still have <code>v17</code> descriptors living outside the datastore -- in Git, in a CI pipeline, in Kubernetes manifests -- <code>POST /api/routes/_from_service_descriptor</code> converts them to Routes without storing anything, one at a time or a whole array at once. <code>POST /api/new/resources</code> does the same for <code>ServiceDescriptor</code> manifests, including multi-document YAML.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="still-the-same-ship">Still the same ship<a href="https://maif.github.io/otoroshi/devmanual/blog/otoroshi-18-new-foundations#still-the-same-ship" class="hash-link" aria-label="Direct link to Still the same ship" title="Direct link to Still the same ship" translate="no">​</a></h2>
<p>Here is the part that matters, and the reason the paradox is a comfort rather than a warning: <strong>the identity of Otoroshi was never in its build tooling.</strong> It is in your routes, your plugins, your API keys, your TLS material, your exporters, your admin API calls, your GitOps pipelines. None of that changed. The proxy engine that handles your requests is the same engine, with the same plugin pipeline, on the same entities.</p>
<p>We did not use the port as an excuse to redesign anything. That was deliberate. A migration this size is dangerous exactly when it becomes a rewrite, so the rule for the port was: change the foundation, change nothing else. What is new in this release came afterwards, one preview at a time, on a foundation that had already proven it could carry the old weight. The only removal is the one we had announced four years in advance.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-to-expect-next">What to expect next<a href="https://maif.github.io/otoroshi/devmanual/blog/otoroshi-18-new-foundations#what-to-expect-next" class="hash-link" aria-label="Direct link to What to expect next" title="Direct link to What to expect next" translate="no">​</a></h2>
<p>The point of all this work is what comes after it. Being back on supported upstreams means security patches flow again without archaeology; it means the JVM libraries we depend on publish for us; and it means the fixes we had been holding until the port landed are merged and shipping -- OpenSearch support, ACME chain selection, WebSocket ordering and chunking fixes, Redis TLS material from config, Elastic exporters that actually report their failures, and more.</p>
<p>It also means we can start using Scala 3 properly rather than merely compiling with it. You will see that show up gradually, in internals first.</p>
<p>In the shorter term, the <code>17.x</code> line stays available for anyone who needs more time.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="try-it">Try it<a href="https://maif.github.io/otoroshi/devmanual/blog/otoroshi-18-new-foundations#try-it" class="hash-link" aria-label="Direct link to Try it" title="Direct link to Try it" translate="no">​</a></h2>
<p><code>18.0.0</code> is out now, on <a href="https://hub.docker.com/r/maif/otoroshi" target="_blank" rel="noopener noreferrer" class="">Docker Hub</a> and in the <a href="https://github.com/MAIF/otoroshi/releases" target="_blank" rel="noopener noreferrer" class="">GitHub releases</a>. The full milestone is <a href="https://github.com/MAIF/otoroshi/milestone/141?closed=1" target="_blank" rel="noopener noreferrer" class="">here</a>.</p>
<p>If you run Otoroshi in production, this is a genuinely useful moment to test an upgrade on a copy of your datastore and tell us what you find -- especially the Service Descriptor migration, which rewrites your data, and the new consumer model if you publish APIs. Open an <a href="https://github.com/MAIF/otoroshi/issues" target="_blank" rel="noopener noreferrer" class="">issue</a> or come talk to us on <a href="https://discord.gg/dmbwZrfpcQ" target="_blank" rel="noopener noreferrer" class="">Discord</a>.</p>
<p>The planks are new. It is still the same ship, and it never stopped sailing.</p>]]></content:encoded>
            <category>otoroshi</category>
            <category>release</category>
            <category>scala3</category>
            <category>play</category>
            <category>pekko</category>
            <category>migration</category>
        </item>
        <item>
            <title><![CDATA[User Analytics & Dashboards - live observability, natively inside Otoroshi]]></title>
            <link>https://maif.github.io/otoroshi/devmanual/blog/user-analytics-and-dashboards</link>
            <guid>https://maif.github.io/otoroshi/devmanual/blog/user-analytics-and-dashboards</guid>
            <pubDate>Thu, 28 May 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[For a long time, getting a clear picture of what was actually happening on your gateway meant plugging Otoroshi into an external observability stack -- Elastic, Kafka, Grafana, you name it -- then maintaining yet another set of dashboards, queries and alert rules far away from where your routes and APIs actually live.]]></description>
            <content:encoded><![CDATA[<p>For a long time, getting a clear picture of what was actually happening on your gateway meant plugging Otoroshi into an external observability stack -- Elastic, Kafka, Grafana, you name it -- then maintaining yet another set of dashboards, queries and alert rules far away from where your routes and APIs actually live.</p>
<p>We thought this should be a lot simpler. So we built <strong>User Analytics &amp; Dashboards</strong>: a complete analytics stack that lives directly inside Otoroshi, on top of a dedicated PostgreSQL data exporter, with curated queries, a beautiful grid of widgets and a threshold-based alert engine. No external stack to plug, no extra UI to learn.</p>
<p><img decoding="async" loading="lazy" alt="User Dashboards overview" src="https://maif.github.io/otoroshi/devmanual/assets/images/user-dashboard-view-506a604707bf39d4850d1d54b540338d.png" width="1424" height="967" class="img_ev3q"></p>
<!-- -->
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="built-where-your-gateway-already-lives">Built where your gateway already lives<a href="https://maif.github.io/otoroshi/devmanual/blog/user-analytics-and-dashboards#built-where-your-gateway-already-lives" class="hash-link" aria-label="Direct link to Built where your gateway already lives" title="Direct link to Built where your gateway already lives" translate="no">​</a></h2>
<p>The whole feature is designed around one idea: the people who configure routes, APIs and API keys in Otoroshi are also the ones who need to see how that traffic behaves -- and they should not have to leave the product to do it.</p>
<p>Concretely, that means:</p>
<ul>
<li class="">A <strong>PostgreSQL data exporter</strong> dedicated to analytics events, with the schema, indexes and alert log table created automatically</li>
<li class="">A <strong>catalogue of curated queries</strong> -- volume, performance, errors, top consumers -- ready to plot or alert on</li>
<li class="">A <strong>grid of widgets</strong> (line, area, bar, pie, donut, big number, table, heatmap) with a point-and-click editor and drag-and-drop reordering</li>
<li class="">A <strong>threshold alert engine</strong> that rides on the same query catalogue, with conditions, AND/OR logic, severities, cooldown and a full event log</li>
<li class="">Everything <strong>tenant-scoped</strong>, so each team only sees its own traffic</li>
</ul>
<p>Five default dashboards are seeded for you the moment you mark the PostgreSQL exporter as active: a <strong>Global Overview</strong>, plus dashboards focused on <strong>Performance</strong>, <strong>Errors</strong>, <strong>APIs &amp; Routes</strong> and <strong>Consumers</strong>. You can use them as-is, fork them or build your own from scratch.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="three-minutes-to-go-live">Three minutes to go live<a href="https://maif.github.io/otoroshi/devmanual/blog/user-analytics-and-dashboards#three-minutes-to-go-live" class="hash-link" aria-label="Direct link to Three minutes to go live" title="Direct link to Three minutes to go live" translate="no">​</a></h2>
<p>There are really only three steps to get going:</p>
<ol>
<li class=""><strong>Plug a PostgreSQL store.</strong> Go to <em>Data Exporters → Add → User Analytics (PostgreSQL)</em>, fill in the connection details, set a retention window, save.</li>
<li class=""><strong>Mark it as the active analytics exporter.</strong> From that moment on, every gateway event flows into your store.</li>
<li class=""><strong>Open the dashboards</strong> under <em>Features → Analytics → User Analytics</em>.</li>
</ol>
<p>That's it -- you are live, with five dashboards already showing real traffic.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="a-dashboard-editor-built-for-iteration">A dashboard editor built for iteration<a href="https://maif.github.io/otoroshi/devmanual/blog/user-analytics-and-dashboards#a-dashboard-editor-built-for-iteration" class="hash-link" aria-label="Direct link to A dashboard editor built for iteration" title="Direct link to A dashboard editor built for iteration" translate="no">​</a></h2>
<p>Every dashboard ships with an <em>Edit mode</em> that unlocks a small toolbar. From there, adding a widget is a matter of picking a query from the catalogue, choosing the visual style and dropping it on the grid. Widths and heights are adjustable, format hints (count, req/s, ms, bytes, percent) are auto-suggested but always overridable, and reordering is pure drag-and-drop.</p>
<p>A few touches we are particularly happy with:</p>
<ul>
<li class=""><strong>Save view as default</strong>: capture the current time range, filters and refresh interval as the dashboard's opening view -- anyone clicking the dashboard from the menu lands on that exact configuration</li>
<li class=""><strong>Drill-down on click</strong>: click a route, API or API key in any top-N chart and the whole dashboard reroutes onto that entity</li>
<li class=""><strong>Deep links</strong>: every filter and time range lives in the URL, so a copied link reproduces the exact same view on the other side</li>
<li class=""><strong>Compare period overlay</strong> on time series, for one-click week-over-week or day-over-day checks</li>
<li class=""><strong>Auto-refresh</strong> at the cadence you want, paused automatically when the tab is hidden</li>
</ul>
<p>The query catalogue covers volume (total / per second, by status, by method, traffic in/out), performance (averages, p50/p75/p95/p99, overhead, latency heatmap), errors (rate, top failing routes) and top consumers (routes, APIs, API keys, end users, domains, countries). Every query supports the same filters -- route, API, API key, group, time window -- so you can scope any of them to a specific slice of your traffic.</p>
<p>And because Otoroshi extensions can register their own analytics queries, anything you build on top of the gateway -- a WAF, an MCP integration, a billing module -- can contribute its own queries that appear in the widget picker and the alert conditions editor with no extra wiring.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="alerts-on-the-same-queries">Alerts, on the same queries<a href="https://maif.github.io/otoroshi/devmanual/blog/user-analytics-and-dashboards#alerts-on-the-same-queries" class="hash-link" aria-label="Direct link to Alerts, on the same queries" title="Direct link to Alerts, on the same queries" translate="no">​</a></h2>
<p>Once you have a dashboard you trust, the natural next step is "tell me when this widget looks bad". That's exactly what the alert engine does: it rides on the same query catalogue your dashboards use, so anything you can plot, you can alert on.</p>
<p>Creating an alert is a small form: a name, a severity (<code>info</code> / <code>warning</code> / <code>critical</code>), a time window, and one or more conditions. Each condition is a one-liner -- <em>"take query <code>error_rate_ts</code>, reduce with <code>max</code>, fire if the result is greater than 0.05"</em> -- with an optional scope on a specific route, API, API key or group. Conditions are combined with AND/OR logic in a visual editor, with a JSON view always one click away for power users.</p>
<p><img decoding="async" loading="lazy" alt="Alert condition editor" src="https://maif.github.io/otoroshi/devmanual/assets/images/user-alert-edit-e0320ae0705a6f8627e0d18dd49c39ac.png" width="1420" height="966" class="img_ev3q"></p>
<p>You also get the boring-but-essential knobs you actually need in production: an <strong>evaluation interval</strong> to control how often the alert is re-checked, a <strong>cooldown</strong> to avoid spamming, and a <strong>window</strong> to control how far back the condition looks.</p>
<p>Where do alerts go? Through the standard Otoroshi alert pipeline. Every firing emits a recognizable <code>AlertEvent</code> (signature <code>alert: "UserAnalyticsAlert"</code>), which means <strong>any Otoroshi data exporter can deliver it</strong> -- mailer for email, webhooks for PagerDuty / Opsgenie / Slack, Kafka for a downstream pipeline, a custom WASM exporter for anything bespoke. The User Alerts page even reminds you to plug one the first time you create an alert and offers a one-click shortcut that pre-fills the filter.</p>
<p>Every firing is also persisted in a per-alert event log, with severity, message and a snapshot of which conditions matched (observed value vs configured threshold). You can mark events as seen one by one, or wipe the inbox with a single <em>Mark all seen</em>.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="tenant-aware-by-design">Tenant-aware by design<a href="https://maif.github.io/otoroshi/devmanual/blog/user-analytics-and-dashboards#tenant-aware-by-design" class="hash-link" aria-label="Direct link to Tenant-aware by design" title="Direct link to Tenant-aware by design" translate="no">​</a></h2>
<p>Multi-tenancy was a non-negotiable from day one. A tenant-admin sees only the traffic, dashboards, alerts and events of their own tenant -- the SQL filter is injected at the data layer, so one tenant's data physically never leaks into another's query. Super-admins switch tenants from the usual dropdown, and the analytics views follow.</p>
<p>Dashboards and alerts are first-class Otoroshi entities, so they participate in your existing export / import / GitOps workflows. Want to ship a dashboard from staging to production? Same flow as any other entity.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="why-this-matters">Why this matters<a href="https://maif.github.io/otoroshi/devmanual/blog/user-analytics-and-dashboards#why-this-matters" class="hash-link" aria-label="Direct link to Why this matters" title="Direct link to Why this matters" translate="no">​</a></h2>
<p>There is a reason most teams end up with a separate observability stack: building this kind of feature is real work. But the cost of <em>running</em> a separate stack is also real -- another product to deploy, another query language, another set of credentials, another place where the data model drifts from your gateway's reality.</p>
<p>User Analytics tries to close that gap. The queries know about your routes, APIs and API keys because they live in the same database as your gateway events. The alert conditions reference exactly the queries you are already plotting. The export pipeline is the same one you already use for emails, Slack or webhooks. It's <em>one</em> product to learn, configured <em>once</em>.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="where-to-next">Where to next<a href="https://maif.github.io/otoroshi/devmanual/blog/user-analytics-and-dashboards#where-to-next" class="hash-link" aria-label="Direct link to Where to next" title="Direct link to Where to next" translate="no">​</a></h2>
<ul>
<li class=""><strong>Try it now</strong>: head to <em>Data Exporters → Add → User Analytics (PostgreSQL)</em>, then to <em>Features → Analytics → User Analytics</em>.</li>
<li class=""><strong>Read the full guide</strong>: the <a class="" href="https://maif.github.io/otoroshi/devmanual/docs/topics/user-analytics">User Analytics topic</a> covers every widget, every query and every knob in detail.</li>
<li class=""><strong>Share what you build</strong>: if you assemble a dashboard or an alert recipe that you think other teams would benefit from, ping us on <a href="https://discord.gg/dmbwZrfpcQ" target="_blank" rel="noopener noreferrer" class="">Discord</a> -- we'd love to feature it.</li>
</ul>
<p>We've been using these dashboards internally for a while now, and it's hard to imagine going back to flipping between Otoroshi and an external tool just to know how the gateway is doing. We hope you'll feel the same.</p>]]></content:encoded>
            <category>otoroshi</category>
            <category>analytics</category>
            <category>dashboards</category>
            <category>alerts</category>
            <category>observability</category>
        </item>
        <item>
            <title><![CDATA[A fresh coat of paint - new dark and light themes for the Otoroshi UI]]></title>
            <link>https://maif.github.io/otoroshi/devmanual/blog/new-ui-themes</link>
            <guid>https://maif.github.io/otoroshi/devmanual/blog/new-ui-themes</guid>
            <pubDate>Fri, 24 Apr 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[The Otoroshi backoffice has been with us for a long time, and over the years it grew organically -- screen by screen, feature by feature. The result was a UI that worked, but felt dated, a little inconsistent from one page to another, and frankly a bit harsh on the eyes (especially in light mode, where staring at a wall of pure white is no one's idea of fun).]]></description>
            <content:encoded><![CDATA[<p>The Otoroshi backoffice has been with us for a long time, and over the years it grew organically -- screen by screen, feature by feature. The result was a UI that worked, but felt dated, a little inconsistent from one page to another, and frankly a bit harsh on the eyes (especially in light mode, where staring at a wall of pure white is no one's idea of fun).</p>
<p>We took the time to refresh both themes from the ground up, and we are pretty happy with the result.</p>
<!-- -->
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="a-new-light-theme-that-doesnt-burn-your-retinas">A new light theme that doesn't burn your retinas<a href="https://maif.github.io/otoroshi/devmanual/blog/new-ui-themes#a-new-light-theme-that-doesnt-burn-your-retinas" class="hash-link" aria-label="Direct link to A new light theme that doesn't burn your retinas" title="Direct link to A new light theme that doesn't burn your retinas" translate="no">​</a></h2>
<p>The light theme has been the most aggressive change. We moved away from the previous high-contrast white-on-white look toward a softer, more neutral palette: gentle off-whites for the background, light gray cards with subtle borders, and just enough contrast to make every block stand out without screaming for attention. Typography has been tightened, spacing rebalanced, and the iconic Otoroshi yellow now plays a clearer accent role instead of fighting with the rest of the page.</p>
<p><img decoding="async" loading="lazy" alt="New light theme - home page" src="https://maif.github.io/otoroshi/devmanual/assets/images/capture-home-light-1d74f34447f3c4d898a0f4c442239fb0.png" width="1551" height="1177" class="img_ev3q"></p>
<p>The result is a UI that you can comfortably look at for hours without feeling like you are staring into a lightbulb -- which, given how much time some of you spend in the dashboard, felt like the least we could do.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="a-dark-theme-worthy-of-the-name">A dark theme worthy of the name<a href="https://maif.github.io/otoroshi/devmanual/blog/new-ui-themes#a-dark-theme-worthy-of-the-name" class="hash-link" aria-label="Direct link to A dark theme worthy of the name" title="Direct link to A dark theme worthy of the name" translate="no">​</a></h2>
<p>The dark theme also got a full makeover. The new palette is a proper deep dark instead of the old half-gray, the contrast on text and metrics is crisp, and the yellow accent really pops against the background. The whole UI feels more modern and more "product-grade".</p>
<p><img decoding="async" loading="lazy" alt="New dark theme - home page" src="https://maif.github.io/otoroshi/devmanual/assets/images/capture-home-dark-54ba60c48d9d9eafbb32e87afd12803f.png" width="1552" height="1176" class="img_ev3q"></p>
<p>It looks great on big monitors, on laptops in the dark, and -- most importantly -- it looks consistent with the light theme. They are now genuinely two facets of the same design system, not two unrelated stylesheets.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="consistent-layouts-across-screens">Consistent layouts across screens<a href="https://maif.github.io/otoroshi/devmanual/blog/new-ui-themes#consistent-layouts-across-screens" class="hash-link" aria-label="Direct link to Consistent layouts across screens" title="Direct link to Consistent layouts across screens" translate="no">​</a></h2>
<p>Refreshing the colors was only half of the work. We have also started to rework individual screens so that the important controls live in predictable places. The general principles we have been moving toward are:</p>
<ul>
<li class="">a clear page header with the entity name and its primary status (state, version, etc.)</li>
<li class="">the <strong>save / publish / primary action</strong> sits in a consistent area at the top</li>
<li class="">a structured left sidebar dedicated to navigation inside the entity (dashboard, routes, backends, plans, ...)</li>
<li class="">a <strong>contextual right panel</strong> when the screen has natural follow-up actions, instead of scattering buttons throughout the page</li>
</ul>
<p>You can see this on the new API editor, where the main panel keeps the live metrics and health summary, while the right rail surfaces the obvious next steps ("Build your API": plugin chains, backends, endpoints):</p>
<p><img decoding="async" loading="lazy" alt="New API editor" src="https://maif.github.io/otoroshi/devmanual/assets/images/capture-api-0762b492180f162593082852d154b189.png" width="1548" height="1040" class="img_ev3q"></p>
<p>The same logic applies to more complex screens like the workflow designer, where the canvas takes center stage and the controls (Save, run, debug, zoom) are anchored in stable positions you can build muscle memory around:</p>
<p><img decoding="async" loading="lazy" alt="New workflow editor" src="https://maif.github.io/otoroshi/devmanual/assets/images/capture-workflows-b6e1949255b65c1309331e14efee8d4a.png" width="1550" height="1178" class="img_ev3q"></p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="whats-next">What's next<a href="https://maif.github.io/otoroshi/devmanual/blog/new-ui-themes#whats-next" class="hash-link" aria-label="Direct link to What's next" title="Direct link to What's next" translate="no">​</a></h2>
<p>This is an ongoing effort. Not every screen has been migrated yet, and we will keep rolling out the new look and the consistent layout pattern in upcoming releases. If you spot a screen that still feels off, or a button that lives in a surprising place, please open an issue or ping us on <a href="https://discord.gg/dmbwZrfpcQ" target="_blank" rel="noopener noreferrer" class="">Discord</a> -- this is exactly the kind of feedback we are looking for.</p>
<p>We hope you enjoy the new themes as much as we enjoyed building them. Now go ahead and try them out -- your eyes will thank you.</p>]]></content:encoded>
            <category>otoroshi</category>
            <category>ui</category>
            <category>design</category>
        </item>
        <item>
            <title><![CDATA[Welcome to the Otoroshi Blog]]></title>
            <link>https://maif.github.io/otoroshi/devmanual/blog/welcome</link>
            <guid>https://maif.github.io/otoroshi/devmanual/blog/welcome</guid>
            <pubDate>Mon, 16 Mar 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[Welcome to the official Otoroshi blog! This is the place where we will share news, release announcements, tutorials, and insights about Otoroshi and the API gateway ecosystem.]]></description>
            <content:encoded><![CDATA[<p>Welcome to the official Otoroshi blog! This is the place where we will share news, release announcements, tutorials, and insights about Otoroshi and the API gateway ecosystem.</p>
<!-- -->
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-to-expect">What to expect<a href="https://maif.github.io/otoroshi/devmanual/blog/welcome#what-to-expect" class="hash-link" aria-label="Direct link to What to expect" title="Direct link to What to expect" translate="no">​</a></h2>
<p>Here are some of the topics we plan to cover:</p>
<ul>
<li class=""><strong>Release announcements</strong> — details about new versions, features, and improvements</li>
<li class=""><strong>Tutorials</strong> — step-by-step guides to help you get the most out of Otoroshi</li>
<li class=""><strong>Architecture deep dives</strong> — technical articles about how Otoroshi works under the hood</li>
<li class=""><strong>Community highlights</strong> — showcasing how the community uses Otoroshi</li>
</ul>
<p>Stay tuned for more content, and feel free to join us on <a href="https://discord.gg/dmbwZrfpcQ" target="_blank" rel="noopener noreferrer" class="">Discord</a> to discuss any topic!</p>]]></content:encoded>
            <category>otoroshi</category>
            <category>announcement</category>
        </item>
    </channel>
</rss>
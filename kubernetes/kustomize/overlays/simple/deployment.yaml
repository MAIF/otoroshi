# External-facing Service (provider-managed LoadBalancer).
# The Deployment + internal Service + Certificate come from
# `components/single-deployment`.
apiVersion: v1
kind: Service
metadata:
  name: otoroshi-external-service
spec:
  type: LoadBalancer
  selector:
    run: otoroshi-deployment
  ports:
  - port: 80
    name: "http"
    targetPort: "http"
  - port: 443
    name: "https"
    targetPort: "https"
